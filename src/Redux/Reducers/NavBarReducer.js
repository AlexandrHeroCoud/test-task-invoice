

const stateInitDefault = {
    pages:['terminals','buyers']
}
const NavBarReducer = (state = stateInitDefault, action) => {
    switch (action.type) {
        default:
            return state
    }
}


export default NavBarReducer